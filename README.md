ruby_interpreter
================

*Usage mode*
Run the following commands in the console:

sh clean.sh
sh compile.sh
./interpreter
"< statements...  >"
CTRL-D                // To close the interpreter, if no error occurs.
